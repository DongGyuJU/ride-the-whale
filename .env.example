# KRX 데이터 접근 — http://data.krx.co.kr 회원가입 후 발급
# .env 파일로 복사 (cp .env.example .env) 후 실제 값 입력
# ⚠️ 비밀번호에 ! 나 $ 들어가도 그대로 적기 (따옴표 NO)
KRX_ID=your_krx_id
KRX_PW=your_krx_password
LOG_LEVEL=INFO

TELEGRAM_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id